# ── proyecto_emociones_vision ────────────────────────────────
opencv-python==4.10.0.84          # Captura de cámara y procesamiento de frames
mediapipe==0.10.14                # Face Mesh: detección de 468 puntos faciales
deepface==0.0.93                  # Clasificación de emociones (usa modelos pre-entrenados)
tf-keras                          # Backend requerido por DeepFace (compatible TF2)
streamlit==1.35.0                 # Dashboard web interactivo
streamlit-webrtc==0.47.6          # Acceso a cámara desde Streamlit en el navegador
av==12.0.5                        # Requerido por streamlit-webrtc para procesar video
plotly==6.8.0                   # Gráficos de barras animados para emociones

# ── proyecto_calidad_agua ─────────────────────────────────────
flask==3.0.3                      # Backend que recibe lecturas del sensor de pH y las guarda en SQLite
pyserial==3.5                     # Lee los datos del ESP32 por el puerto USB (sin necesidad de WiFi)
requests==2.32.3                  # Envía las lecturas leídas del serial hacia el backend Flask
pandas==2.2.2                     # Manejo de series de tiempo de sensores
# (Streamlit ya está declarado arriba)
# ── proyecto_smartstand_ble ───────────────────────────────────
# (paho-mqtt, pandas, numpy, streamlit y plotly ya están declarados arriba)
seaborn==0.13.2                   # Mapas de calor de aforo en la feria
matplotlib==3.9.0                 # Soporte adicional para visualizaciones